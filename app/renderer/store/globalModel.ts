const globalModel = {
  namespace: 'globalModel',
  openSeamlessImmutable: true,
  state: {
    appName: 'CV MKr',
  },
  // rc-redux-model: 在 model 中，action 以及 reducer 均可忽略不写。只需要定义好 state 值即可。
};

export default globalModel;
